# プロジェクト企画書：ちびキャラちょっかいアプリ

## 概要

カメラで人差し指を検知し、Canvasに線を引くことでちびキャラをインタラクティブに操作できるWebアプリ。
誘導・閉じ込め・こかすなど、複数のインタラクションを楽しめる。
大学の講義（動画・CG・インタラクション）の制作課題として開発。

---

## リポジトリ構成

```
chibi-interaction/
├── index.html
├── style.css
├── src/
│   ├── main.js          # エントリポイント・初期化
│   ├── character.js     # ちびキャラのステート・AI・アニメーション
│   ├── line.js          # 描画線の管理・衝突判定
│   ├── hand.js          # Mediapipe Hands ラッパー
│   └── renderer.js      # Canvas描画ループ
├── assets/
│   └── sprites/         # キャラクタースプライト画像
└── README.md
```

---

## 本番アプリ：ちびキャラちょっかいアプリ

### コンセプト

Webカメラに映る人差し指でCanvasに線を引き、ちびキャラをインタラクティブに操作する。
線を使ってキャラを誘導・閉じ込め・こかすことができる。

### 使用技術

| 技術 | 用途 |
|------|------|
| Mediapipe Hands | 人差し指のリアルタイム検出 |
| Canvas 2D API | 線描画・キャラ描画・フレームループ |
| バニラ JavaScript | ゲームロジック全般 |
| HTML5 `<video>` | カメラ映像取得 |

### インタラクション仕様

#### 線の描画
- 人差し指の先端座標をMediapipeで毎フレーム取得
- 指が画面上で動くと連続した線を描画
- 線は一定時間（例：5秒）で徐々にフェードアウト

#### キャラクターの行動

| 状態 | 条件 | 挙動 |
|------|------|------|
| 通常 | 線がない・遠い | ランダムウォーク |
| 回避 | 線が一定距離内 | 線から遠ざかる方向へ移動 |
| 閉じ込め | 線に囲まれた領域内 | 脱出口を探して右往左往 |
| こける | 指先と直接衝突 | こけるアニメーション再生・一定時間行動停止 |
| 回復 | こけから一定時間経過 | 起き上がりアニメーション → 通常に戻る |

#### 閉じ込め判定
- 描画済みの線群から閉領域を近似検出
- キャラ位置が閉領域内かを判定（Point-in-Polygon アルゴリズム または 線の密度マップで近似）

### 今後の拡張候補

- キャラの感情表現（表情の変化）
- 複数キャラの同時出現・キャラ同士のインタラクション
- react-three-fiberによる3D化
- スコアシステム（閉じ込め時間・こかした回数）

---

## プロトタイプ：ダンゴムシシミュレーション

### 目的

本番アプリ開発前に、キャラクター挙動のコアロジックを小さく検証する。
Mediapipeは使わず、マウス操作で代替し、動きの質とCanvasレンダリングを先に固める。

### 使用技術

| 技術 | 用途 |
|------|------|
| Canvas 2D API | ダンゴムシ描画・ライン描画 |
| バニラ JavaScript | シミュレーションロジック |
| マウスイベント | 線描画の入力（指検知の代替） |

### ダンゴムシの仕様

#### 形状
- 楕円を数珠つなぎにしたセグメント構造（例：7〜9セグメント）
- 先頭セグメントが大きく、後ろに向かって小さくなる
- 触角を2本描画

#### 行動ロジック

| 状態 | 挙動 |
|------|------|
| 通常移動 | 進行方向にわずかなランダム角度変化を加えながら前進（ランダムウォーク） |
| 壁反射 | Canvas端に当たると跳ね返る |
| 線回避 | 線分との距離を計算し、近づくと進行方向を線から遠ざかる方向に補正 |
| 丸まり | 線に囲まれた・強く接触した場合、球状にまるまるアニメーション |
| 丸まり解除 | 一定時間後に展開して通常移動に戻る |

#### 移動実装方針
- 各フレームで先頭セグメントの位置・角度を更新
- 後続セグメントは前のセグメントを一定距離で追従（連鎖IK的な動き）

### Canvas構成

```
[ カメラ映像 or 背景 ]   ← 最下層
[ 描画線レイヤー ]        ← 中間層
[ キャラクターレイヤー ]  ← 最上層
```

---

## 開発フェーズ

```
Phase 1：プロトタイプ（ダンゴムシシミュレーション）
  - Canvas上でダンゴムシが動く
  - マウスで線を引くと回避する
  - 丸まり挙動の実装と調整

Phase 2：本番アプリへの移植
  - ダンゴムシ → ちびキャラへの置き換え
  - マウス入力 → Mediapipe Hands への置き換え
  - カメラ映像を背景に合成

Phase 3：ブラッシュアップ
  - 閉じ込め判定の精度向上
  - 感情表現・複数キャラ対応
  - UIデザイン整備
```

---

## ブランチ運用方針

| ブランチ | 用途 |
|----------|------|
| `main` | 安定版・デモ用 |
| `dev` | 開発統合ブランチ |
| `feature/xxx` | 機能単位の作業ブランチ |

コミットメッセージの形式：
```
feat: ダンゴムシの回避ロジック実装
fix: Canvas端での反射バグ修正
refactor: characterクラスをモジュール化
docs: README更新
```
